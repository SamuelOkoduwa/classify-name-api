const express = require('express');

const router = express.Router();

const { healthCheck, classifyName  } = require('./controller');

router.get('/health', healthCheck);
router.get('/classify', classifyName);

module.exports = router;